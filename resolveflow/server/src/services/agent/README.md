# Member 2 — Agent Orchestration
# Owner: feature/member-2-agent
# 
# Place agent service files here:
#   orchestrator.js   — main agent loop
#   goalParser.js     — extract intent from customer goal
#   toolSelector.js   — decide which tool to call
#   planner.js        — plan next step given observations
#   failureDetector.js — detect action failures
#   replanner.js      — select alternative resolution
#   escalationHandler.js — trigger escalation
