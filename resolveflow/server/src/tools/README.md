# Member 2 — Agent Tools
# Owner: feature/member-2-agent
#
# Place tool wrapper files here:
#   customer.tool.js    — wraps customerService.getById()
#   order.tool.js       — wraps orderService.getById()
#   inventory.tool.js   — wraps inventoryService.checkAvailability()
#   policy.tool.js      — wraps policyService.checkEligibility()
#   refund.tool.js      — wraps actionsService.refund()
#   replacement.tool.js — wraps actionsService.replacement()
#   cancel.tool.js      — wraps actionsService.cancel()
#   verification.tool.js— wraps verificationService.run()
